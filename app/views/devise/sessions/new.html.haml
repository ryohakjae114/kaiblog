= title t('.title')
= simple_form_for(resource, url: session_path(resource_name)) do |f|
  = f.input :name
  = f.input :password, required: true
  = f.button :submit, t('.title')

  = link_to t('devise.registrations.new.title'), new_user_registration_path, class: 'd-block mt-2'
